# Portfolio System

A comprehensive Laravel-based portfolio management system.

## Setup

1. Install dependencies: `composer install` and `npm install`
2. Create `.env` file and configure database
3. Run migrations: `php artisan migrate`
4. Build assets: `npm run dev`

## Features

- Project portfolio management
- Performance monitoring
- Excel import/export
- Activity logging
- User permissions and authentication
